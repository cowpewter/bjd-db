export const typeDef = `
type Subscriptions {
  security: Boolean!
  newsletter: Boolean!
  dollComment: Boolean!
  albumComment: Boolean!
}
`;

// default resolver is sufficient
export const resolver = {};
